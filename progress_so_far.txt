================================================================================
LEFT — SMART EXPENSE TRACKER : PROGRESS SO FAR
Last updated: 2026-09-13 (after Phase 4 — Budgeting)
Repo: https://github.com/satyam-dev-singh/Expense-Tracker-APP (branch main)
================================================================================

WHAT IS BUILT (DONE)
--------------------------------------------------------------------------------
PHASE 0 — PROJECT FOUNDATION
[✓] Gradle project (Kotlin 2.0.21, AGP 8.7.3, Compose M3, KSP), version catalog,
    wrapper config, debug/release types, ProGuard rules, GitHub Actions CI
[✓] Package structure core/ + feature/, Hilt DI (6 modules), navigation
    skeleton (8 routes incl. detail + budgets), splash routing via DataStore flag
[✓] Design system: original spruce palette, typography (moneyHero 44sp),
    tokens, buttons/fields/card, LeftIcons conventions
[✓] Manifest: zero runtime permissions, allowBackup=false, cleartextTraffic=false

PHASE 1 — LOCAL DATA LAYER
[✓] Room left.db v1: 6 entities, 6 DAOs (full §12 capability set, Flow reads),
    6 repositories, 11 use cases, Money value class (integer minor units,
    overflow-checked), MonthRange half-open epoch-day ranges, idempotent seeding
[✓] Data integrity: category FK SET_NULL, archive-first, unique (year,month)
    budget index, no destructive migration fallback
[✓] Tests: 5 JVM classes + fakes; 2 instrumented Room classes
[✓] Verification: harness 61/61; structural 186/186; defect fixed: "Rs. 10" parse

PHASE 2 — ONBOARDING
[✓] Five-step flow (Welcome → Currency → Income → Budget → Categories) with
    progress indicator, back/skip, per-field inline validation
[✓] CompleteOnboarding use case: validation BEFORE any write; writes profile →
    current-month budget → recurring income → category archives → flags, with
    the completion flag ALWAYS LAST (safe retry on any failure)
[✓] Skip semantics, category archiving, currency chips + custom ISO code,
    leap-safe income next-occurrence math per frequency
[✓] Tests: CompleteOnboardingTest + OnboardingViewModelTest + fakes with event
    ordering; harness 19/19 PASSED

PHASE 3 — CORE TRANSACTION EXPERIENCE
[✓] Dashboard (FR-02): money-left hero (income − expenses), income/spent row,
    budget context, recent 5 (tappable), quick add — reactive, UiState pattern
[✓] Add transaction (FR-03/S07): amount-first form, segmented expense/income,
    category chips, merchant/note, date picker (UTC-day mapping), recurring flag,
    inline errors, double-tap guard, saves via AddTransaction (source=MANUAL)
[✓] Transactions list (FR-11/S10): month navigation, all-time text search
    (escaped DAO LIKE), type/category/amount-range filters, day grouping
    (Today/Yesterday/date), friendly empty states
[✓] Transaction detail (S09): view + in-place edit (prefill format→parse
    round-trips exactly) + delete with confirmation; streams never clobber edits
[✓] Shared: TransactionRow, TransactionFormFields, budgetUsagePercentage
    single-source helper, nav route transactions/detail/{id}
[✓] Tests: 4 ViewModel suites + shared MainDispatcherRule; harness 24/24 PASSED

PHASE 4 — BUDGETING
[✓] Budget domain (BudgetUseCases.kt): evaluateBudgetStatus (80% warn / 100%
    exceeded), dailyAllowance pure fn (budget remaining ÷ days left including
    today, integer floor, zero-clamped overspend, current-month only), and the
    CalculateDailyAllowance use case over repositories + injected clock
[✓] Dashboard: month navigation (year-rollover safe), pacing status line
    (On track / Approaching budget limit / Over budget), daily allowance line
    for the current month only, clickable budget card → Budgets, "Set a budget"
    empty state
[✓] Budgets screen (S14): total monthly budget (blank clears it), per-category
    limits for all expense categories, per-category spent-this-month context,
    month navigation with ROLLOVER prefill from the previous month (notice
    shown, never silently saved), inline validation, save via repositories
[✓] Tests: BudgetUseCasesTest (thresholds, allowance math incl. leap Feb, last
    day, clamps, gating), BudgetViewModelTest (prefill, rollover, save/clear,
    category limits, validation), DashboardViewModelTest extended (month nav,
    allowance gating, status escalation)
[✓] Verification: BudgetHarness 19/19 PASSED; regressions 61/61 + 19/19 +
    24/24 PASSED; structural 241/241 PASSED (95 Kotlin files)

WHAT IS LEFT TO BUILD (NOT STARTED)
--------------------------------------------------------------------------------
[ ] PHASE 5 — VOICE (NEXT): mic permission, SpeechRecognizer, rule-based parser,
    category matching, confirmation screen, duplicate protection
[ ] PHASE 6 — ANALYTICS: category distribution, trends, month comparison
[ ] PHASE 7 — SUBSCRIPTIONS + NOTIFICATIONS: CRUD UI, renewal calc,
    WorkManager reminders, budget alerts, summaries
[ ] PHASE 8 — ANDROID-NATIVE: widget, shortcuts, QS tile, biometric lock
[ ] PHASE 9 — CLOUD (optional): auth, remote DB, sync engine, backup/restore
[ ] PHASE 10 — AI: structured extraction, smart categorization, insights
[ ] PHASE 11 — QA: UI tests, accessibility pass, offline + migration tests,
    performance, security review
[ ] PHASE 12 — RELEASE: signing, Play Console, privacy policy, store listing

IMMEDIATE NEXT STEP ON A DEV MACHINE
--------------------------------------------------------------------------------
    ./gradlew lintDebug testDebugUnitTest assembleDebug
(First run downloads Gradle 8.11.1 + dependencies.)
================================================================================
