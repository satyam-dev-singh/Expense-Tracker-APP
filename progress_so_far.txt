================================================================================
LEFT — SMART EXPENSE TRACKER : PROGRESS SO FAR
Last updated: 2026-09-13 (after Phase 2 — Onboarding)
Repo: https://github.com/satyam-dev-singh/Expense-Tracker-APP (branch main)
================================================================================

WHAT IS BUILT (DONE)
--------------------------------------------------------------------------------
PHASE 0 — PROJECT FOUNDATION
[✓] Gradle project: Kotlin 2.0.21, AGP 8.7.3, KSP, Jetpack Compose + Material 3,
    version catalog, Gradle 8.11.1 wrapper config, debug/release build types,
    ProGuard rules, GitHub Actions CI (lint + unit tests + assembleDebug)
[✓] Package structure: com.left.app → core/ (common, data, database, datastore,
    designsystem, di, domain, model, navigation, network, security, utils)
    + feature/ (splash, onboarding, dashboard, transactions, analytics, settings)
[✓] Hilt DI: AppModule (Clock, app scope), DatabaseModule, DataStoreModule,
    RepositoryModule, OnboardingPreferencesModule
[✓] Navigation skeleton: Splash, Onboarding, Home, Transactions, AddTransaction,
    Analytics, Settings + bottom nav + centered Add FAB; splash routes
    onboarding↔home via the DataStore completion flag
[✓] Design system: original spruce/neutral palette (light+dark), typography scale
    (moneyHero 44sp), spacing/shape/elevation tokens, buttons/fields/card,
    LeftIcons conventions with safe fallback
[✓] Manifest: zero runtime permissions, allowBackup=false, cleartextTraffic=false

PHASE 1 — LOCAL DATA LAYER
[✓] Room left.db v1: 6 entities, 6 DAOs (full §12 capability set, Flow reads),
    6 repositories, 11 use cases, Money value class (integer minor units,
    overflow-checked), MonthRange (half-open epoch-day ranges), idempotent
    default-category seeding (deterministic IDs + INSERT OR IGNORE)
[✓] Data integrity: category FK onDelete=SET_NULL, archive-first lifecycle,
    unique (year,month) budget index, no destructive migration fallback
[✓] DataStore: onboarding flag + default currency (OnboardingPreferences
    contract for testability)
[✓] Tests: 5 JVM test classes + fakes; 2 instrumented Room test classes
[✓] Verification: JVM harness 61/61 PASSED; structural 186/186 PASSED;
    defect found+fixed: "Rs. 10" parse stray dot

PHASE 2 — ONBOARDING
[✓] Five-step flow: Welcome(name) → Currency → Income → Budget → Categories
    (UX/UI Spec S02–S05, PRD FR-01) with progress indicator + back/skip
[✓] CompleteOnboarding use case: validation BEFORE any write (blank name,
    invalid ISO 4217 currency, non-positive amounts rejected), writes profile →
    current-month budget → recurring income → category archives → flags, with
    the completion flag ALWAYS LAST so failures never leave a half-onboarded app
[✓] Skip semantics: income/budget steps optional; skipping clears inputs so
    nothing is persisted; categories step archives deselected defaults
[✓] OnboardingViewModel state machine (StateFlow, immutable state, double-finish
    guard, SafeLogger on failure, user-safe error messages only)
[✓] Currency step: common one-tap currencies + custom ISO code validation
[✓] Income next-occurrence math per frequency (weekly/biweekly/monthly/
    quarterly/yearly/one-time) with leap-safe java.time arithmetic
[✓] Tests: CompleteOnboardingTest (ordering/skip/validation/frequency dates,
    Dec-31 stays December), OnboardingViewModelTest (step flow, validation,
    skip, completion, double-finish), new fakes with shared event recorder
[✓] Verification: OnboardingHarness 19/19 PASSED (JVM mirror of the rules)

WHAT IS LEFT TO BUILD (NOT STARTED)
--------------------------------------------------------------------------------
[ ] PHASE 3 — CORE TRANSACTIONS (NEXT): dashboard wiring (money left,
    income/spent, recent list), amount-first add form, transaction list/detail,
    edit, delete, search/filter
[ ] PHASE 4 — BUDGETING: total + category budgets, progress UI, warning
    thresholds, daily allowance, month rollover
[ ] PHASE 5 — VOICE: mic permission, SpeechRecognizer, rule-based parser,
    category matching, confirmation screen, duplicate protection
[ ] PHASE 6 — ANALYTICS: category distribution, trends, month comparison
[ ] PHASE 7 — SUBSCRIPTIONS + NOTIFICATIONS: CRUD UI, renewal calc,
    WorkManager reminders, budget alerts, summaries
[ ] PHASE 8 — ANDROID-NATIVE: widget, shortcuts, QS tile, biometric lock
[ ] PHASE 9 — CLOUD (optional): auth, remote DB, sync engine, backup/restore
[ ] PHASE 10 — AI: structured extraction, smart categorization, insights
[ ] PHASE 11 — QA: UI tests, accessibility pass, offline + migration tests,
    performance, security review
[ ] PHASE 12 — RELEASE: signing, Play Console, privacy policy, store listing

IMMEDIATE NEXT STEP ON A DEV MACHINE
--------------------------------------------------------------------------------
    ./gradlew lintDebug testDebugUnitTest assembleDebug
(First run downloads Gradle 8.11.1 + dependencies.)
================================================================================
