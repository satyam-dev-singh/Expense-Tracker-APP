<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Privacy-first (PRD §21 / Technical Architecture §7):
      Phase 0–1 request ZERO runtime permissions. Microphone (voice) is added in
      Phase 5 when the feature exists; notifications in Phase 7. Never request
      contacts, location, SMS, or unnecessary storage access.
    -->

    <application
        android:name=".LeftApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Left">

        <!--
          allowBackup=false: financial data must not silently leave the device
          via platform auto-backup. Explicit, user-controlled backup/restore is
          planned for Phase 9 (see ARCHITECTURE.md "Decisions").
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
